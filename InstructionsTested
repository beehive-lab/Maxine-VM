DONE: 
movt,movw,mov32BitConstant, push,pop,movror,add,strd,ldrd
ldrb
ldrshw
addq
subq
incq
decq
ldr
str
mov
vmov
vldr
vstr
vmul
vadd vsub vdiv vpush vpop




jcc and jmp look ok but I will try to contrive an example ...

NOT TESTED but built on tested functionality ...
nop is not tested but it is merely a mov r12,r12 ...
alignforPatchableDirectCall included for backwards compatibility, it inserts nops that
do mov r12,r12 so it should be ok.

Please note add merely adds a constant to a register
addq actually adds 2 registers together ... addq actually calls addRegister ...

TO BE TESTED:

jcc,jmp,,enter,leave,nullcheck,ret,
align,ucomisd, pushq,popq,xorq,cmpl,movslq,
call,
, setUpScratch,cmp, movsd.movss,swi,
,sub,eor,addclsl,patchJumpTarget,branch,
membarrier!!!
mul div not implemented/tested

