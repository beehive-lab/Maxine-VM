application.title=MaxwellInspector
application.vendor=doug
build.classes.dir=${build.dir}/classes
build.classes.excludes=**/*.java,**/*.form
# This directory is removed when the project is cleaned:
build.dir=build
build.generated.dir=${build.dir}/generated
build.generated.sources.dir=${build.dir}/generated-sources
# Only compile against the classpath explicitly listed here:
build.sysclasspath=ignore
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
debug.classpath=\
    ${run.classpath}
debug.test.classpath=\
    ${run.test.classpath}
# This directory is removed when the project is cleaned:
dist.dir=dist
dist.jar=${dist.dir}/Inspector.jar
dist.javadoc.dir=${dist.dir}/javadoc
excludes=
includes=**
jar.compress=true
javac.classpath=\
    ${reference.Base.jar}:\
    ${reference.Assembler.jar}:\
    ${reference.VM.jar}:\
    ${reference.Tele.jar}:\
    ${reference.VMDI.jar}
# Space-separated list of extra javac options
javac.compilerargs=
javac.deprecation=false
javac.source=1.6
javac.target=1.6
javac.test.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}:\
    ${libs.junit_4.classpath}
javadoc.additionalparam=
javadoc.author=false
javadoc.encoding=
javadoc.noindex=false
javadoc.nonavbar=false
javadoc.notree=false
javadoc.private=false
javadoc.splitindex=true
javadoc.use=true
javadoc.version=false
javadoc.windowtitle=
# Property libs.junit_4.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.junit_4.classpath=../../netbeans-6.0.1/java1/modules/ext/junit-4.1.jar
main.class=com.sun.max.ins.MaxineInspector
manifest.file=manifest.mf
meta.inf.dir=${src.dir}/META-INF
platform.active=default_platform
project.Assembler=../Assembler
project.Base=../Base
project.Tele=../Tele
project.VM=../VM
project.VMDI=../VMDI
reference.Assembler.jar=${project.Assembler}/dist/Assembler.jar
reference.Base.jar=${project.Base}/dist/Base.jar
reference.Tele.jar=${project.Tele}/dist/Tele.jar
reference.VM.jar=${project.VM}/dist/VM.jar
reference.VMDI.jar=${project.VMDI}/dist/VMDI.jar
run.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
# Space-separated list of JVM arguments used when running the project
# (you may also define separate properties like run-sys-prop.name=value instead of -Dname=value
# or test-sys-prop.name=value to set system properties for unit tests):
run.jvmargs=-d64 -Dsun.reflect.noInflation=false -Dsun.reflect.inflationThreshold=1000000 -Dsun.misc.ProxyGenerator.saveGeneratedFiles=true -Xmx3g
run.test.classpath=\
    ${javac.test.classpath}:\
    ${build.test.classes.dir}
source.encoding=UTF-8
src.src.dir=src
test.test.dir=test
