Rules for every compiler
------------------------


Every compiler must eliminate checkcasts referring to Word subtypes. 
Executing them in the target JVM would cause crashing, since Words are not objects.

After any invocation of any method of UnsafeLoophole there may ber a checkcast.
It must be eliminated.


Rules for every optimizing compiler
-----------------------------------

